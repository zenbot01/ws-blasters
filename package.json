{
  "name": "ws-blasters",
  "private": true,
  "version": "0.2.0",
  "description": "Single-player blaster game (HTML Canvas)",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint .",
    "format": "prettier -w ."
  },
  "devDependencies": {
    "eslint": "^9.19.0",
    "prettier": "^3.4.2",
    "vitest": "^2.1.8"
  }
}
